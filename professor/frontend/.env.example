# ============================================================================
# professor / frontend
# ============================================================================
# Variáveis REACT_APP_* são EMBUTIDAS NO BUNDLE em tempo de build e ficam
# publicamente legíveis. Nunca coloque segredo aqui.

PORT=3002
REACT_APP_BACKEND_URL=http://localhost:8001
GENERATE_SOURCEMAP=false
