# Runtime do backend do aluno — apenas o que o processo importa.
#
# Separado das ferramentas de desenvolvimento em requirements-dev.txt: a imagem
# de producao nao precisa de pytest, mypy, black nem pandas, e cada pacote a
# mais e tempo de build, tamanho de imagem e superficie de vulnerabilidade.
#
# Removidos por nao serem importados em lugar nenhum do runtime:
#   boto3, pandas, numpy, jq, typer, python-jose, passlib, requests-oauthlib,
#   google-cloud-storage (o app do aluno usa Firestore, nunca Storage — os
#   artefatos binarios sao do pipeline, que permanece privado).

# --- Web ---
fastapi==0.110.1
uvicorn==0.25.0
python-multipart>=0.0.9

# --- Modelos e validacao ---
pydantic>=2.6.4
email-validator>=2.2.0

# --- Persistencia ---
motor==3.3.1
pymongo==4.6.3

# --- Autenticacao ---
bcrypt==4.1.3
pyjwt>=2.10.1

# --- Firebase / Firestore ---
firebase-admin>=6.5.0
google-cloud-firestore>=2.11.0

# --- Gemini API (substitui o proxy emergentintegrations, removido 2026-08-21) ---
google-genai>=2.12.1

# --- Figuras de questão (proxy servidor-a-servidor para o pipeline) ---
httpx==0.28.1

# --- Suporte ---
python-dotenv>=1.0.1
tzdata>=2024.2

# --- Mercado Pago (loja de Sparks) ---
mercadopago==3.5.0

# --- Corretor de redação (Nivel 2 local — checagem ortografica de COMP-I) ---
pyspellchecker>=0.8.1
